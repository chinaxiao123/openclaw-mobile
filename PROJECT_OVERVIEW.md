# OpenClaw Mobile - 项目总览

在安卓手机上本地部署 OpenClaw 的完整解决方案。

## 🎯 项目目标

让用户在安卓手机上安装一个 APP，自动完成：
1. ✅ 检测/安装 Termux
2. ✅ 自动部署 OpenClaw Gateway
3. ✅ 安装所有依赖（Python、Node.js、Agent Reach 等）
4. ✅ 启动服务并返回连接信息
5. ✅ 内嵌客户端界面，直接使用

## 📁 项目结构

```
openclaw-mobile/
├── lib/
│   ├── main.dart                          # App 入口
│   ├── providers/
│   │   ├── config_provider.dart           # 配置管理
│   │   └── session_provider.dart          # 会话管理
│   ├── services/
│   │   ├── termux_deploy_service.dart     # Termux 部署服务
│   │   ├── gateway_service.dart           # Gateway 连接服务
│   │   └── websocket_service.dart         # WebSocket 客户端
│   └── screens/
│       ├── splash_screen.dart             # 启动页
│       ├── onboarding/
│       │   ├── onboarding_screen.dart     # 向导主屏
│       │   ├── welcome_screen.dart        # 欢迎页
│       │   ├── gateway_config_screen.dart # 远程配置
│       │   ├── local_deploy_screen.dart   # 本地部署
│       │   └── complete_screen.dart       # 完成页
│       ├── home_screen.dart               # 主页（底部导航）
│       ├── chat_screen.dart               # 聊天界面
│       ├── sessions_screen.dart           # 会话列表
│       ├── agent_status_screen.dart       # Agent 状态
│       └── settings_screen.dart           # 设置
├── termux/
│   ├── install.sh                         # Termux 部署脚本
│   └── README.md                          # 脚本说明
├── android/
│   ├── README.md                          # 原生集成说明
│   └── (Flutter 生成的 Android 项目)
├── ios/
│   └── (Flutter 生成的 iOS 项目)
├── pubspec.yaml                           # Flutter 依赖配置
└── README.md                              # 项目说明
```

## 🚀 使用流程

### 用户视角

```
1. 安装 OpenClaw APK
       ↓
2. 打开 APP → 欢迎页
       ↓
3. 选择部署方式
   ├─ 本地部署（推荐）→ 自动安装 Termux → 自动部署 → 完成
   └─ 远程配置 → 输入 Gateway 地址 → 完成
       ↓
4. 进入主界面 → 开始使用
```

### 技术流程

```
┌─────────────────────────────────────────────────────────┐
│  Flutter APP                                            │
│                                                         │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐ │
│  │  欢迎/引导  │ →  │  本地部署   │ →  │  主界面     │ │
│  │             │    │  或远程配置 │    │  (聊天等)   │ │
│  └─────────────┘    └─────────────┘    └─────────────┘ │
└─────────────────────────────────────────────────────────┘
         │                      │
         │ (Platform Channel)   │ (WebSocket)
         ▼                      ▼
┌─────────────────┐    ┌─────────────────────────────────┐
│  Android 原生   │    │  Termux (Linux 环境)            │
│  - Termux 检测  │    │  ├─ Python 3.12                 │
│  - 脚本执行     │    │  ├─ Node.js LTS                 │
│  - 后台服务     │    │  ├─ OpenClaw Gateway            │
│  - 保活         │    │  ├─ Agent Reach                 │
└─────────────────┘    │  └─ 各种工具链                  │
                       └─────────────────────────────────┘
```

## 🛠️ 开发指南

### 环境要求

- Flutter SDK 3.16+
- Dart 3.2+
- Android Studio / VS Code
- Android SDK 26+
- Termux (测试用)

### 快速开始

```bash
# 1. 克隆项目
cd openclaw-mobile

# 2. 获取依赖
flutter pub get

# 3. 运行（连接手机或启动模拟器）
flutter run

# 4. 构建 Release APK
flutter build apk --release
```

### 测试 Termux 部署

```bash
# 在 Termux 中手动测试部署脚本
curl -fsSL https://raw.githubusercontent.com/your-repo/openclaw-mobile/main/termux/install.sh | bash
```

## 📦 核心功能

### 1. 本地部署（Termux）

- 自动检测 Termux 安装状态
- 一键安装 Termux（跳转应用商店）
- 推送并执行部署脚本
- 实时监控部署进度
- 自动保存连接信息

### 2. 远程配置

- 输入 Gateway 地址/端口/Token
- 测试连接
- 保存配置

### 3. 聊天对话

- 实时 WebSocket 通信
- 消息历史
- 打字动画

### 4. Agent 管理

- 查看运行状态
- 重启服务
- 查看日志

### 5. 后台保活

- 前台服务通知
- 电池优化白名单
- 开机自启（可选）

## 🔐 安全考虑

- Token 本地加密存储
- WebSocket 连接支持 WSS
- 不收集用户数据
- 开源可审计

## 📱 支持的安卓版本

- Android 8.0+ (API 26+)
- 推荐 Android 10+

## 🐛 已知问题

1. 部分手机后台会被杀（需要手动设置白名单）
2. 首次部署需要 3-5 分钟（依赖下载安装）
3. 部分工具在 ARM 架构下可能需要编译

## 📄 许可证

MIT License

---

_让 AI Agent 真正随身而行_
